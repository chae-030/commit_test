import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <h1 className="text-3xl font-KELSI grow text-center">
      <Link to="/">COMMIT</Link>
    </h1>
  );
};

export default Logo;
